"""Serve a local build to a LAN device over an HTTP proxy, terminating TLS with
real certificates so the device trusts it natively (no CA install), plus an
injected debug toolbar and mobile DevTools (eruda)."""

__version__ = "0.1.0"
